services:
  - type: worker
    name: betfetcher-worker
    env: python
    runtime: python3
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
      - key: ADMIN_ID
      - key: UPDATE_INTERVAL
        value: "60"
      - key: API_URL
        value: ""
