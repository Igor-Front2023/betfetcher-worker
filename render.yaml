services:
  - type: web
    name: betfetcher-bot
    env: python
    plan: free
    region: frankfurt  # можно заменить на nearest (Render сам выберет)
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py

    envVars:
      - key: BOT_TOKEN
        sync: false       # 🔒 Вводишь вручную в Render Dashboard
      - key: ADMIN_ID
        sync: false       # 🔒 Тоже вводишь вручную
      - key: API_URL
        sync: false       # 🔒 Вводишь вручную или оставляешь пустым
      - key: UPDATE_INTERVAL
        value: "180"      # Интервал проверки API в секундах (3 мин)
      - key: PORT
        value: "10000"    # Render сам передает порт через переменную, но укажем запасной

