services:
  - type: web
    name: betfetcher-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        value: "placeholder"
      - key: ADMIN_ID
        value: "placeholder"
      - key: UPDATE_INTERVAL
        value: "180"
      - key: API_URL
        value: ""
